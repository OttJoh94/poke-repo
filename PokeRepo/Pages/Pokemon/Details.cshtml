@page
@using PokeRepo.Models
@model PokeRepo.Pages.Pokemon.DetailsModel
@{
	
	@if(Model.Pokemon != null)
	{
		<h1>@Model.Pokemon.Name</h1>
		<h2>Height: @Model.Pokemon.Height</h2>
		<h2>Weight: @Model.Pokemon.Weight</h2>
		<h3>Forms:</h3>
		foreach (Form form in Model.Pokemon.Forms)
		{
			<h3>@form.Name</h3>
		}
		<h3>Species:@Model.Pokemon.Species.Name</h3>

		<form method="post">
		<input type="submit" asp-page-handler="AddMyPokemon" asp-route-name="@Model.Pokemon.Name" value="Add to my pokemons" class="btn btn-success" />
		</form>

		<br />
		@if(Model.PokemonAdded)
		{
			<h4>@Model.Pokemon.Name added to your PokéDex</h4>
		}

	}
	else
	{
		<h1>Can't find that pokemon. Misspelled??</h1>
	}

}
